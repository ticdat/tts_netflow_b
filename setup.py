import tts_netflow_b
from setuptools import setup, find_packages

setup(
   name = 'tts_netflow_b',
   packages = find_packages(),
   version = tts_netflow_b.__version__,
   author = 'Pete Cacioppi',
   url = 'https://github.com/ticdat/tts_netflow_b',
   platforms = 'any',
)